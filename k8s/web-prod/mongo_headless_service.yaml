apiVersion: v1
kind: Service
metadata:
  name: thoas-mongodb-svc
  namespace: ensembl-apps
  labels:
    name: database
spec:
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
  selector:
    app: database
