apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql-core-deployment
spec:
  replicas: 6
  template:
    spec:
      containers:
        - name: graphql-core
          env:
            # Keeps GraphiQL static/sdl URLs under the same ingress prefix.
            - name: GRAPHIQL_BASE_PATH
              value: /api/graphql/core
