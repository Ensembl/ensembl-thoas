apiVersion: v1
kind: Secret
metadata:
  name: mongo-config
type: Opaque
data:
  # Base64 encoded values
  host: HOST_NAME(S)_SEPARATEDBY_COMMA
  port: PORT
  db: DATABASE_NAME
  user: DB_USER
  password: DB_PASS
  collection: DB_COLLECTION
