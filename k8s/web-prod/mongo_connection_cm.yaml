apiVersion: v1
# host refers to a Service that routes traffic outside k8s
data:
  mongo.conf: |
    [MONGO DB]
    user = admin
    password = password
    host = mongo
    port = 27017
    db = apps_data
    collection = graphql-test-6
    [GRPC]
    host = localhost
    port = 50051
kind: ConfigMap
metadata:
  name: thoas-configmap
  namespace: ensembl-apps
